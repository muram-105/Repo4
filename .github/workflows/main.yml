name: Set up script

on:
  push:
    branches:
      - main

jobs:
  set-up-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo3
        uses: actions/checkout@v2
        with:
          repository: muram-105/repo3
          ref: main

      - name: Install script dependencies
        run: |
          # Install dependencies for your script here

      - name: Set up script
        run: |
          ls
          cp -r account_pdcs/* repo4/
      - name: Commit changes to repo4
        run: |
          cd repo4/
          git config user.name "Gmuram-105"
          git config user.email "muram8700@gmail.com"
          git add .
          git commit -m "Sync changes from repo3"
          git push origin main

