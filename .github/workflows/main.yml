name: Set up script

on:
  push:
    branches:
      - main

jobs:
  set-up-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo3
        uses: actions/checkout@v2
        with:
          repository: muram-105/repo3
          ref: main

      - name: Install script dependencies
        run: |
          # Install dependencies for your script here

      - name: Set up script
        run: |
          ls
